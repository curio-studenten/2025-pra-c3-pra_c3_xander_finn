<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pra_c3_winui.TeamsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="20" Margin="0,0,0,20">
            <TextBlock Text="👥 Teams" FontSize="28" FontWeight="Bold"/>
            <Button x:ConnectionId='5'
                x:Name="RefreshButton"
                Content="🔄 Vernieuwen"
                                           />
            <ProgressRing x:ConnectionId='6'
                x:Name="LoadingRing"
                IsActive="False"
                Width="24"
                Height="24"/>
        </StackPanel>

        <!-- Teams Grid -->
        <GridView x:ConnectionId='2'
            Grid.Row="1"
            x:Name="TeamsGridView"
            SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20"
                        Width="250"
                        Margin="8">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="Punten:" Opacity="0.7"/>
                                <TextBlock Text="{Binding Points}" FontWeight="Bold"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Empty State -->
        <StackPanel x:ConnectionId='3'
            x:Name="EmptyState"
            Grid.Row="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Visibility="Collapsed">
            <TextBlock Text="👥" FontSize="64" HorizontalAlignment="Center"/>
            <TextBlock Text="Nog geen teams" FontSize="18" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>

