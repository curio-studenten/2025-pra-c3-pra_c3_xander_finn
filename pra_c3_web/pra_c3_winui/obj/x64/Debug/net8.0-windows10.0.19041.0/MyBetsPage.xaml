<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pra_c3_winui.MyBetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Mijn Weddenschappen" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Bekijk al je geplaatste weddenschappen"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <Button x:ConnectionId='9' Grid.Column="1" Content="Vernieuwen"                            
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Totaal Ingezet" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:ConnectionId='8' x:Name="TotalBetText" Text="€0.00" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="Totaal Gewonnen" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:ConnectionId='7' x:Name="TotalWonText" Text="€0.00" FontSize="24" FontWeight="Bold"
                               Foreground="Green"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Lopende Weddenschappen" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:ConnectionId='6' x:Name="PendingCountText" Text="0" FontSize="24" FontWeight="Bold"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bets List -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16">
            <Grid>
                <ListView x:ConnectionId='2' x:Name="BetsListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="12" Margin="0,4"
                                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                  CornerRadius="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Match -->
                                <TextBlock Grid.Column="0" Grid.Row="0"
                                           Text="{Binding MatchDisplay}"
                                           FontSize="16" FontWeight="SemiBold"/>

                                <!-- Bet Info -->
                                <TextBlock Grid.Column="0" Grid.Row="1"
                                           FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    <Run Text="Voorspelling: "/>
                                    <Run Text="{Binding DisplayBet}" FontWeight="SemiBold"/>
                                    <Run Text=" | Inzet: €"/>
                                    <Run Text="{Binding Amount}"/>
                                    <Run Text=" ("/>
                                    <Run Text="{Binding Odds}"/>
                                    <Run Text="x)"/>
                                </TextBlock>

                                <!-- Status -->
                                <TextBlock Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                                           Text="{Binding DisplayStatus}"
                                           VerticalAlignment="Center"
                                           Margin="20,0"/>

                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock x:ConnectionId='3' x:Name="NoBetsText"
                           Text="Je hebt nog geen weddenschappen geplaatst."
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="Collapsed"/>

                <ProgressRing x:ConnectionId='4' x:Name="LoadingRing" IsActive="False"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>

