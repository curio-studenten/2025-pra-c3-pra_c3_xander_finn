<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pra_c3_winui.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="{ThemeResource AccentFillColorDefaultBrush}" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="20">
                <TextBlock Text="⚽ Schoolvoetbal Gokken" FontSize="24" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:ConnectionId='6' x:Name="UserInfoText" FontSize="14"
                           Foreground="White" VerticalAlignment="Center" Opacity="0.9"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                <TextBlock x:ConnectionId='4' x:Name="CreditsText" FontSize="16" FontWeight="SemiBold"
                           Foreground="White" VerticalAlignment="Center"/>
                <Button x:ConnectionId='5' Content="Uitloggen"                           />
            </StackPanel>
        </Grid>

        <!-- Content -->
        <NavigationView x:ConnectionId='2' Grid.Row="1" x:Name="NavView"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="Top"
                                                                   >
            <NavigationView.MenuItems>
                <NavigationViewItem Content="🎰 Wedden" Tag="betting"/>
                <NavigationViewItem Content="📋 Mijn Weddenschappen" Tag="mybets"/>
                <NavigationViewItem Content="📊 Resultaten" Tag="results"/>
            </NavigationView.MenuItems>

            <Frame x:ConnectionId='3' x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Page>

